# Алгоритми сортування 
*Проблема сортування* –  добре відома проблема програмування, з якою стикаються розробники програмного забезпечення та фахівці з обробки даних.<br> 
*Основна мета задачі сортування* - упорядкувати набір об'єктів у певному порядку.<br> 
*Алгоритм сортування* - це чітка послідовність обчислювальних кроків, яка приймає на вхід хаотичний набір даних (масив, вектор тощо) і перевпорядковує його елементи за певним критерієм (найчастіше - за зростанням чи спаданням значень).<br>
<br>
## Головні критерії оцінки алгоритмів:
### Часова складність 
Показує, як швидко зростає кількість операцій (порівнянь та обмінів) із розширенням розміру масиву N. Оцінюється через нотацію Big-O:
* **Квадратичні алгоритми (O(N²)):** Прості, але повільні на великих даних (наприклад, Bubble Sort, Insertion Sort).
* **Логарифмічні алгоритми (O(N log N)):** Ефективні, використовують принцип рекурсивного розбиття задачі (наприклад, Merge Sort, Quick Sort).
* **Лінійні алгоритми (O(N)):** Найшвидші, але працюють лише за певних умов і обмежень на самі дані (наприклад, Counting Sort, Radix Sort).
### Просторова складність  
Оцінює, скільки додаткової оперативної пам'яті вимагає алгоритм для своєї роботи:
* **In-place:** Має складність O(1). Алгоритм переставляє елементи всередині самого початкового масиву, використовуючи лише 1–2 тимчасові змінні (Insertion Sort).
* **Out-of-place:** Вимагає виділення додаткової пам'яті під копії масиву. Наприклад, Merge Sort вимагає O(N) додаткової пам'яті, що може бути критичним при роботі з гігантськими обсягами даних.
### Стабільність 
Стабільним називається алгоритм, який не змінює початкового відносного порядку елементів із однаковими значеннями (Наприклад, Insertion Sort та Merge Sort).
### Адаптивність 
Алгоритм є адаптивним, якщо він враховує поточний стан масиву. Якщо дані вже частково відсортовані, адаптивний алгоритм виконає значно менше операцій і завершить роботу швидше (Наприклад, Insertion Sort).
## Основні класи та підходи до сортування
Усі алгоритми сортування глобально діляться на кілька підходів за логікою своєї роботи:
* **Інкрементні (Ітеративні) методи:** Алгоритм перебирає елементи один за одним і крок за кроком будує відсортовану частину послідовності (Наприклад, Insertion Sort).
* **Розділяй і володарюй (Divide and Conquer):** Велика задача рекурсивно ділиться на менші підзадачі, які сортуються окремо, а потім об'єднуються (Наприклад, Merge Sort, Quick Sort).
* **Гібридні методи:** Сучасні алгоритми, які аналізують вхідні дані й комбінують ітеративні та рекурсивні підходи для досягнення максимальної швидкості в реальних програмах (Наприклад , TimSort).
